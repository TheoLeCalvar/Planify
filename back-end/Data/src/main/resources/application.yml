# Spring properties
spring:
  application:
     name: data.com       # Service registers under this name 
  data:    mongodb:      uri: mongodb://localhost:27017/cliente

# HTTP Server
server:
  port: 3200   # HTTP (Tomcat) port
